generateCase.py 读取case中的xls，生成对应的测试用例xml脚本文件
	1.读取xls文件B列，生成用例的编号、标题；
	2.读取xls文件C、D列，分别生成发票头字段名（C列）与对应的值（D列）；
	3.读取xls文件E、F列（G/H,I/J......），分别生成发票项目明细的字段名（E列）与对应的字段值（F列），可无限往后面扩展，生成多行项目明细；
	4.执行后分别在case、test、check文件夹中生成对应用例的脚本，并在TestCasePool中增加脚本。